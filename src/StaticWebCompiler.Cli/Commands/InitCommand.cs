using Microsoft.Extensions.Logging;

namespace StaticWebCompiler.Cli.Commands;

internal static class InitCommand
{
    public static async Task HandleAsync(string buildPath, ILogger logger)
    {
        return;
    }
}